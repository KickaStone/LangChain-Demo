[project]
name = "langchain-demo"
version = "0.1.0"
description = "A simple LangChain assistant chat demo"
readme = "readme.md"
requires-python = ">=3.11"
dependencies = [
    "langchain>=0.3.0",
    "langchain-openai>=0.3.0",
    "python-dotenv>=1.0.0",
]

[project.scripts]
chat = "main:main"
